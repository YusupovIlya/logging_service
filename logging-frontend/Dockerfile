# Определите базовый образ. Вы можете выбрать конкретную версию Node.js, подходящую для вашего приложения.
FROM node:16

# Установите рабочую директорию внутри контейнера
WORKDIR /app

# Скопируйте файлы package.json и package-lock.json (или yarn.lock, если используется yarn)
COPY package*.json ./

# Установите зависимости приложения
RUN npm install --legacy-peer-deps

# Скопируйте оставшиеся файлы проекта в рабочую директорию контейнера
COPY . .

# Откройте порт, на котором запущено ваше приложение
EXPOSE 3000

# Запустите приложение
CMD ["npm", "start"]
